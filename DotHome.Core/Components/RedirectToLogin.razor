@inject NavigationManager Navigation

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        base.OnAfterRender(firstRender);
        Navigation.NavigateTo($"login?returnUrl={HttpUtility.UrlEncode(Navigation.Uri)}", true);
    }
}